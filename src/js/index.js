var swiper = new Swiper('.swiper-container', {
    slidesPerView: 4,
    autoplay: {
      delay: 3000,
      disableOnInteraction: false,
    },
    spaceBetween: -600,
    loop:true,
    speed: 2000,
    centeredSlides:true
  });